package dev.daniza.portfoliowatcher.model.navigation

enum class NavigationTarget(val label: String) {
    SEARCH("search"),
    DETAIL("detail"),
    LIST("list"),
    FAVORITE("favorite"),
    HOME("home"),
}